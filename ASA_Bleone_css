body {
    background-image: url('noel.jpg'); /* Je définis l'image de fond de la page */
    background-size: cover;
    background-attachment: fixed;
    background-position: center;
}

/*J'initialise les dimensions de la carte (1500 pixels de large et 650 de haut, marges automatiques) */ 
#map
{
width: 1500px;
height: 650px;
margin: auto;
/*margin: auto;*/
}

/* Je définis les caractéristiques de police des entités situées dans la boîte de "map-overlay" */ 
.map-overlay-2 {
            font: 12px/20px 'Helvetica Neue', Arial, Helvetica, sans-serif;
            position: absolute;
            width: 200px; /*Largeur de 200 pixels */
            top: 177px; /* Le haut de la check box se situe à 177 pixels en haut */ 
            right: 50px; /*Je place en haut à droite la check box */
            padding: 10px;
        }

.map-overlay-2 .map-overlay-inner {
            background-color: #ffffff;
            box-shadow:0 1px 2px rgba(0, 0, 0, 0.849);
            border-radius: 3px;
            padding: 10px;
            margin-bottom: 10px;
        }

.map-overlay-inner fieldset {
            border: none;
            padding: 0;
            margin: 0 0 10px;
        }

.map-overlay-inner .categoryLabel {
            display: block;
            font-weight: bold; /* La police de texte des étiquettes est en gras */
            margin: 0 0 5px;
        }

/* Je décide de la couleur du texte "h1" (rouge) et son alignement sur la page (au centre). Un arrière plan est choisi pour que le texte soit plus lisible */       
h1
{
color : darkgreen;
text-align: center;
background-color: rgba(255, 255, 255, 0.9);
padding: 20px 40px;
border-radius: 10px;
box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
display: block;
margin: 20px auto;
width: fit-content;
}

/* Légende RPG */
.map-legend {
    position: absolute;
    left: 53px;
    top: 300px;
    background-color: #ffffff;
    padding: 15px;
    border-radius: 3px;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.849);
    font: 12px/20px 'Helvetica Neue', Arial, Helvetica, sans-serif;
    width: 180px;
}

.map-legend h4 {
    margin: 0 0 10px 0;
    font-size: 14px;
    font-weight: bold;
}

.legend-item {
    display: flex;
    align-items: center;
    margin-bottom: 5px;
}

.legend-color {
    display: inline-block;
    width: 20px;
    height: 20px;
    margin-right: 8px;
}
/* Paramètrage de la légende et ajout des couleurs pour chaque catégorie */

/* J'ajoute un effet d'opacité au survol du logo de l'IUT */
img:hover
{
opacity:0.5;
}

/* J'ajoute un effet de couleur rose au survol de mon nom et prénom au bas de la page */

footer:hover
{
text-decoration: underline;
background-color: pink;	
}

p, footer /* J'applique les marges intérieures (padding) et les marges extérieures (margin) aux deux balises à la fois */

{
padding: 10px;
margin: 30px;	
}
/* Je définis les caractéristiques de l'image pop-up qui s'affichera en carte 3D */
 
#popup-image {
  max-width: 300px; /*  J'ajuste la taille de l'image à afficher dans la carte 3D */
  max-height: 300px; /* J'ajuste la taille de l'image à afficher dans la carte 3D */
  width: auto;
  height: auto;
}
/* Je règle la taille du "logo AMU" (250 pixels de large) */
.logo_amu
{
  width: 250px;
}

footer
{
background-color: rgba(255, 255, 255, 0.9);
padding: 10px 20px;
border-radius: 10px;
box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
display: block;
width: fit-content;
}
/* Arrière plan blanc semi-transparent pour le footer */  